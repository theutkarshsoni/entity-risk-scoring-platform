# entity-risk-scoring-platform
Ingesting activity logs from multiple systems (login events, file access events, privileged command execution, VPN/network access) for a mix of human users and non-human entities (service accounts, API keys, and autonomous agents) and producing a daily risk score per entity that flags anomalous behaviour.
